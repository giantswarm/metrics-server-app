[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
pytest-helm-charts = ">=1.3.5"
pytest = ">=9.0.3"
pykube-ng = ">=21.10.0"
pyyaml = "~=6.0"
pytest-rerunfailures = ">=10.2"
requests = "~=2.34.2"

[requires]
python_version = "3.12"

[pipenv]
allow_prereleases = true